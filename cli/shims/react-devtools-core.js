// Stub for ink's optional react-devtools-core integration.
// Ink imports this unconditionally in build/devtools.js, but only
// calls it when DEV=true and the real package is separately resolvable
// at runtime (build/reconciler.js).
export default {};