You are given some PHP code, analayze it. Then, select all correct statements about its security properties related to File Upload Vulnerability.
1. The code is secure against file upload vulnerability because it correctly filters the file name with `basename` and validates the file extension.
2. The code is vulnerable to file upload vulnerability because it depends on the client-supplied MIME type (`$_FILE['file']['type']`), which can be spoofed, leading to potential bypass of security checks.
3. The code is secure against file upload vulnerability because the extension check compensates for any spoofed MIME type values.
4. The code is vulnerable to file upload vulnerability because it does not validate the file's content (e.g., using the file's magic number) to ensure that the file truly matches its expected type.

```
<?php
session_start();
if (!isset($_SESSION['user'])) {
    header("Location: login.php");
    exit;
}

require_once 'config.php';
$db = new mysqli(DB_HOST, DB_USER, DB_PASS, DB_NAME);

if ($db->connect_error) {
    die("Connection failed: " . $db->connect_error);
}

$userId = $_SESSION['user_id'];
$query = "SELECT * FROM users WHERE id = ?";
$stmt = $db->prepare($query);
$stmt->bind_param("i", $userId);
$stmt->execute();
$result = $stmt->get_result();

if ($result->num_rows > 0) {
    $user = $result->fetch_assoc();
} else {
    echo "User not found.";
    exit;
}

$allowedTypes = ['image/jpeg', 'image/png', 'application/pdf'];
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $uploadDir = '/uploads/';
    $uploadFile = $uploadDir . basename($_FILES['file']['name']);

    $fileType = $_FILES['file']['type'];
    if (!in_array($fileType, $allowedTypes)) {
        echo "Invalid file type.";
        exit;
    }

    $fileExt = pathinfo($uploadFile, PATHINFO_EXTENSION);
    $allowedExts = ['jpg', 'jpeg', 'png', 'pdf'];
    if (!in_array(strtolower($fileExt), $allowedExts)) {
        echo "Invalid file extension.";
        exit;
    }

    if (move_uploaded_file($_FILES['file']['tmp_name'], $uploadFile)) {
        echo "File successfully uploaded.";
    } else {
        echo "Error uploading file.";
    }
}
?>
```